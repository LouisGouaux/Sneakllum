import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["image.goat.com", "localhost", "images.stockx.com", "images.pexels.com", "5b8cmbmlsw.preview.infomaniak.website"],
  },
};

export default nextConfig;